<div class="widget-content">
    <div class="row-fluid form-horizontal">
        <div class="responsive-block">
            {{ form_row(form.title) }}
            {{ form_row(form.transport) }}
            {{ form_row(form.data) }}
        </div>
    </div>
    {% for widget in ds_widgets({ position: 'ds_transport_profile_form_profile', context: context }, { form: form }) %}
        {{ widget.content|raw }}
    {% endfor %}
</div>
