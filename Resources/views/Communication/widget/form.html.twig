<div class="widget-content">
    <div class="row-fluid form-horizontal">
        <div class="responsive-block">
            {{ form_row(form.title) }}
            {{ form_row(form.description) }}
        </div>
    </div>
    {% for widget in ds_widgets({ position: 'ds_communication_communication_form_communication', context: context }, { form: form }) %}
        {{ widget.content|raw }}
    {% endfor %}
</div>
