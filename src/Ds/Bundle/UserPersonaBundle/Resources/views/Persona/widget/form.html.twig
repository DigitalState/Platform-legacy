<div class="widget-content">
    <div class="row-fluid form-horizontal">
        <div class="responsive-block">
            {{ form_row(form.user) }}
            {{ form_row(form.definition) }}
            {{ form_row(form.data) }}
        </div>
    </div>
    {% for widget in ds_widgets({ position: 'ds_userpersona_persona_form_persona', context: context }, { form: form }) %}
        {{ widget.content|raw }}
    {% endfor %}
</div>
