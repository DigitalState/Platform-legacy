ds_topic:
    resource: @DsTopicBundle/Controller
    type: annotation

ds_topic_api_rest:
    resource: @DsTopicBundle/Resources/config/oro/routing/api.yml
    type: rest
    prefix: %ds.api.rest.prefix%
    requirements:
        version: %ds.api.rest.requirements.version%
        _format: json
    defaults:
        version: %ds.api.rest.defaults.version%
