ds_accountbpm_account:
    resource: @DsAccountBpmBundle/Controller/Account
    type: annotation
    prefix: %ds.account.prefix%
    requirements:
        _locale: %ds.account.requirements.locale%
    defaults:
        _locale: %ds.account.defaults.locale%
