ds_accountasset_account:
    resource: @DsAccountAssetBundle/Controller/Account
    type: annotation
    prefix: %ds.account.prefix%
    requirements:
        _locale: %ds.account.requirements.locale%
    defaults:
        _locale: %ds.account.defaults.locale%