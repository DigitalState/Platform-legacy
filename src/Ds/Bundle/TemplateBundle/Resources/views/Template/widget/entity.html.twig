{% import 'OroUIBundle::macros.html.twig' as UI %}

<div class="widget-content">
    <div class="row-fluid form-horizontal">
        <div class="responsive-block">
            {{ UI.renderProperty('ds.template.id.label'|trans, entity.id) }}
            {{ UI.renderProperty('ds.template.title.label'|trans, entity.title) }}
        </div>
    </div>
    {% for widget in ds_widgets({ position: 'ds_template_template_entity_template', context: context }, { entity: entity }) %}
        {{ widget.content|raw }}
    {% endfor %}
</div>
