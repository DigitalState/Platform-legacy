{% import 'OroUIBundle::macros.html.twig' as UI %}

<div class="widget-content">
    <div class="row-fluid form-horizontal">
        <div class="responsive-block">
            {{ UI.renderProperty('ds.communication.template.id.label'|trans, entity.id) }}
            {{ UI.renderProperty('ds.communication.template.title.label'|trans, entity.title) }}
        </div>
    </div>
    {% for widget in ds_widgets({ position: 'ds_communication_template_entity_template', context: context }, { entity: entity }) %}
        {{ widget.content|raw }}
    {% endfor %}
</div>
