ds_accountrecord_account:
    resource: @DsAccountRecordBundle/Controller/Account
    type: annotation
    prefix: %ds.account.prefix%
    requirements:
        _locale: %ds.account.requirements.locale%
    defaults:
        _locale: %ds.account.defaults.locale%
